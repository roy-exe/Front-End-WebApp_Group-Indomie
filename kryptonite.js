
// Custom f(x)
{
  function randB(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.round(Math.floor(Math.random() * (max - min + 1)) + min);
  }

  function randP(arrL) {
    Math.floor(Math.random() * arrL);
  }
}

// !Special f(x)
{
  function verSize() {
    
  }
}

// Custom Local/Global Var, Let, and Const
{
  
}

// !On Trigger/Event Listener
{
  
}